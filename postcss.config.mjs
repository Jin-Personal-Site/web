/** @type {import('postcss-load-config').Config} */
const config = {
	plugins: {
		'@tailwindcss/postcss': {
			// optimize: {
			// 	minify: true,
			// },
		},
		// 'postcss-nested-ancestors': {},
		// autoprefixer: {},
	},
}

export default config
